<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="4dp"
        android:color="@color/green"/> <!-- Cor do contorno quando selecionado -->
    <corners android:radius="16dp"/> <!-- Mantendo os cantos arredondados -->
</shape>
